namespace SkierFramework
{
    //在UIType中定义了的成员，就应该在UIConfig文件中编写相关配置信息，当然还要有对应的预制体。
    public enum UIType
    {
        UILoadingView,
        UILoginView,
        UIMessageBoxView,
        UITestView,
        Max,
    }
}
